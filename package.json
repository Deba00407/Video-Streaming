{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },

  "scripts": {
    "dev": "concurrently --kill-others -n FRONTEND,BACKEND -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  }
}
